{
  "name": "StorySong Dev",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "eamodio.gitlens",
        "esbenp.prettier-vscode"
      ]
    }
  },
  "remoteEnv": {
    "ANTHROPIC_API_KEY": "${localEnv:ANTHROPIC_API_KEY}"
  },
  "postCreateCommand": "mkdir -p .claude && echo '{\"permissions\":{\"defaultMode\":\"bypassPermissions\"}}' > .claude/settings.json && echo 'Container ready! Run: claude'",
  "features": {
    "ghcr.io/devcontainers/features/git:1": {}
  }
}